#!/bin/bash

#!/bim/sh
DATABASE_NAME=""
DB_DUMP_LOCATION=""

psql -d "$DATABASE_NAME" << EOSQL
    GRANT ALL PRIVILEGES ON DATABASE "$DATABASE_NAME" TO postgres;
EOSQL

psql -d "$DATABASE_NAME" < "$DB_DUMP_LOCATION";